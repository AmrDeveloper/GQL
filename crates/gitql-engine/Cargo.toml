[package]
name = "gitql-engine"
authors = ["AmrDeveloper"]
version = "0.9.0"
edition = "2021"
description = "GitQL Engine"
repository = "https://github.com/amrdeveloper/gql/tree/main/crates/gitql-engine"
license = "MIT"

[dependencies]
gitql-ast = { path = "../gitql-ast", version = "0.7.0" }
regex = "1.8.4"
gix = { workspace = true, features = ["blob-diff"] }
